package part._0._3

fun main () {
    /*
        3. 변수
        val = value (값)
        var = variable (변경가능)
     */
    val a : Int = 3
    var b : Int = 10
    
    val name = "이름" // 타입을 명시하지 않아도 타입추론을 한다.
 }